<%- include('../main')%>
<%- include('../partials/navbar.ejs')%>
<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <% edit_links.forEach(function(link) { %>
                        <form action="/links/edit/<%= link.id %>" method="post">
                            <div class="form-group">
                                <input type="text" class="form-control border-dark" name="title" placeholder="Ingresa el título" value="<%= link.title %>" autofocus required>
                            </div>
                            <div class="form-group mt-2">
                                <input type="url" class="form-control border-dark" name="url" placeholder="Ingresa tu URL a guardar" value="<%= link.url %>" required>
                            </div>
                            <div class="form-group mt-2">
                                <textarea class="form-control border-dark" name="description" rows="12" placeholder="Ingresa la descripción del sitio web" required><%= link.description %></textarea>
                            </div> 
                            <div class="form-group mt-2">
                                <button class="btn btn-success mx-auto btn-block w-100">Guardar</button>
                            </div>
                        </form>
                    <% }); %>
                </div>
            </div>
        </div>
    </div>
</div>
</body>
</html>